{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "#/components/schemas/backtest-status",
  "title": "Backtest Status",
  "description": "Status of a backtest job",
  "type": "string",
  "enum": [
    "PENDING",
    "RUNNING",
    "SUCCESS",
    "FAILURE"
  ]
}